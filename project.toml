[project]
name = "dota-analytics-platform"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.poetry]
packages = [{ include = "dota_analytics", from = "src" }]

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"
